services:
  - type: web
    name: kyn-strr-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_TOKEN
        sync: false
      - key: JWT_SECRET
        sync: false
